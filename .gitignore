# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing coverage
coverage

# Vitest browser-mode attachments. The visual regression run writes the actual
# and diff images here on a mismatch. Only the baselines under
# src/**/__screenshots__/ are committed, and only ever by CI.
.vitest-attachments

# Images the visual-diff job gathers for its workflow artifact.
visual-diff-report

# Claude Code local (personal, per-session) settings
.claude/settings.local.json