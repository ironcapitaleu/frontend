# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing coverage
coverage

# Vitest browser-mode attachments.
.vitest-attachments

# Story images the visual-diff job renders, and the report it builds from them.
# None of this is ever committed: the job renders both sides of the comparison
# on every run, so no screenshot lives in the repository.
.visual-run
visual-diff-report

# Claude Code local (personal, per-session) settings
.claude/settings.local.json