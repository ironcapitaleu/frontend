import { EmptyPanel } from "./EmptyPanel";

/** The Relationships tab of the company page. It stays empty until its tab ticket fills it. */
export function RelationshipsTab() {
	return <EmptyPanel tab="relationships" />;
}
